@model IEnumerable<districtProject.Models.District>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.district_name_thai)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.district_name_eng)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MultipleDistrict.multiple_district_name_thai)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.district_name_thai)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.district_name_eng)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MultipleDistrict.multiple_district_name_thai)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.district_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.district_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.district_id })
        </td>
    </tr>
}

</table>
